<core:FragmentDefinition xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:tnt="sap.tnt" xmlns:core="sap.ui.core">
    <Dialog id="prodTable" title="{i18n>product}">
        <Table class="sapUiSizeCompact" id="idProdTablePopout" selectionChange="singlemode" 
        mode="SingleSelectMaster" width="auto" sticky="ColumnHeaders,HeaderToolbar" growing="true" growingScrollToLoad="true" growingThreshold="8" items="{oModel>/ProductData}">
            <headerToolbar>
                <Toolbar>
                    <SearchField width="50%" change="onSearchProductPop" />
                    <ToolbarSpacer />
                    <Button icon="sap-icon://edit" tooltip="{i18n>editProduct}" press="handleEditProductPopout" />
                    <Button icon="sap-icon://delete" tooltip="{i18n>delP}"  press="onDeleteProductPopout" />
                    <Button icon="sap-icon://broken-link" tooltip="{i18n>unBind}"  press="onUnbindProductPopout" />
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column mergeDuplicates="true" hAlign="Begin" >
                    <header>
                        <Text text="{i18n>productID}" />
                    </header>
                </Column>
                <Column demandPopin="true" hAlign="Begin">
                    <header>
                        <Text text="{i18n>productName}" />
                    </header>
                </Column>
                 <Column hAlign="Center">
                    <header>
                        <Text text="{i18n>shipTo}" />
                    </header>
                </Column>
            </columns>
            <ColumnListItem>
                <Text text="{oModel>Product}" />
                <Text text="{oModel>ProductName}" />
                <ObjectStatus text="{oModel>ShiptoCount}" />
            </ColumnListItem>
        </Table>
        <beginButton>
            <Button text="{i18n>btnAddNew}" type="Emphasized" press="handleProductSelectDialogPress" />
        </beginButton>
        <endButton>
            <Button text="{i18n>cancel}" press="onCloseProd" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
