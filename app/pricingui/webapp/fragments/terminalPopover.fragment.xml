<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
    <Popover showHeader="false" contentWidth="20rem" contentHeight="15rem" placement="Bottom" ariaLabelledBy="master-title">
        <NavContainer>
            <Page class="sapUiResponsivePadding--header" title="{i18n>terminals}">
                <List items="{/results}">
                    <StandardListItem title="{Terminal}" description="{TerminalName}" />
                </List>
            </Page>
        </NavContainer>
    </Popover>
</core:FragmentDefinition>
