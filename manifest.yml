# Generated manifest.yml based on template version 0.1.0
# appName = pricingNotification
# language=nodejs
# multitenancy=false
---
applications:
# -----------------------------------------------------------------------------------
# Backend Service
# -----------------------------------------------------------------------------------
- name: pricingNotification-srv
  random-route: true  # for development only
  path: gen/srv
  memory: 256M
  buildpack: nodejs_buildpack
  services:
    - pricingNotification-xsuaa-service
    - dev_job_scheduler
  env:
    SAP_JWT_TRUST_ACL: >
        [
          {"clientid":"*", "identityzone":"*"}
        ]    

