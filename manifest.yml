# Generated manifest.yml based on template version 0.1.0
# appName = pricingNotificationV2
# language=nodejs
# multitenancy=false
---
applications:
# -----------------------------------------------------------------------------------
# Backend Service
# -----------------------------------------------------------------------------------
- name: pricingNotificationV2-srv
  random-route: true  # for development only
  path: gen/srv
  memory: 1024M
  buildpack: nodejs_buildpack
  services:
    - pricingNotificationV2-xsuaa-service
    - JS_PricingNotification
  env:
    SAP_JWT_TRUST_ACL: >
        [
          {"clientid":"*", "identityzone":"*"}
        ]    

